## Vector + Redpanda 

Запуск:

```bash
docker compose -f redpanda-cluster.yaml up
```

В Grafana (будет доступна на порту `3000`) нужно импортировать Dashboard с id `18135` чтобы увидеть статистику кластера RedPanda.
